import SwiftUI

struct HomeView: View {
    
//    let modelData: [Weather] = LentaService().getAll()
    
    var body: some View {
        Text("Home page.")
    }
}

struct HomeView_Previews: PreviewProvider {
    static var previews: some View {
        HomeView()
    }
}
